import React from 'react';
import Navigation from '../components/Navigation';

const contact = () => {
    return (
        <div>
            <Navigation/>
            <h4>contact</h4>
        </div>
    );
};

export default contact;